# 🚀 자취연구소 배포 완전 가이드 (비개발자용 스텝바이스텝)

> 이 문서는 코딩 모르는 분도 따라올 수 있게 **클릭 하나씩** 순서대로 설명합니다.
> 예상 소요시간: **약 2시간** (한 번만 하면 됨)

---

## 🗺️ 전체 지도

```
[내 컴퓨터]  →  [GitHub]  →  [Vercel(무료 호스팅)]  →  [실제 웹사이트]
     ↓
   수익화 연결
     ├─ Google AdSense (광고)
     ├─ 쿠팡 파트너스 (커미션)
     ├─ Google Search Console (구글 검색 노출)
     └─ Naver Search Advisor (네이버 검색 노출)
```

---

## 🎯 PART 1 — 로컬에서 사이트 띄워보기 (15분)

### Step 1. Node.js 설치

1. https://nodejs.org/ko 접속
2. 왼쪽의 **"LTS"** 버튼 (녹색) 클릭해서 다운로드
3. 설치 프로그램 실행 → **Next → Next → Install** (기본값 유지)
4. 설치 완료 후 **"명령 프롬프트"** 실행 (윈도우키 → `cmd` 검색)
5. 아래 명령어 입력, 버전 숫자가 나오면 성공:

```bash
node -v
npm -v
```

### Step 2. 프로젝트 폴더로 이동

명령 프롬프트에서:

```bash
cd "C:\Users\박대홍\OneDrive\바탕 화면\스터디\웹사이트\자취연구소"
```

### Step 3. 패키지 설치 (처음 1회)

```bash
npm install
```

5~10분 걸립니다. 커피 한 잔 ☕

### Step 4. 로컬에서 실행

```bash
npm run dev
```

메시지에 `http://localhost:3000` 가 보이면 성공!  
브라우저에서 접속해보세요. 사이트가 떠야 합니다.

▶ **중단하려면**: 명령 프롬프트에서 `Ctrl + C`

---

## 🎯 PART 2 — GitHub에 올리기 (15분)

### Step 1. GitHub 가입

1. https://github.com → Sign up
2. 이메일, 비밀번호, 사용자명 입력

### Step 2. Repository 만들기

1. 로그인 후 오른쪽 상단 **"+"** → **New repository**
2. Repository name: `single-lab`
3. **Private** 선택 (나만 보이게)
4. 가장 아래 **Create repository** 클릭

### Step 3. GitHub Desktop으로 올리기 (가장 쉬움)

1. https://desktop.github.com/ 에서 앱 다운로드 & 설치
2. GitHub 계정으로 로그인
3. 상단 메뉴 **File → Add Local Repository**
4. `C:\Users\박대홍\OneDrive\바탕 화면\스터디\웹사이트\자취연구소` 폴더 선택
5. "이 폴더는 git 저장소가 아니에요" 하면 **create a repository** 클릭 → **Create Repository**
6. 상단 **Publish repository** 버튼 → **Keep this code private** 체크 → **Publish Repository**

---

## 🎯 PART 3 — Vercel에 배포 (10분, 무료)

### Step 1. Vercel 가입

1. https://vercel.com → **Sign Up**
2. **Continue with GitHub** 선택 (GitHub 계정으로 로그인)

### Step 2. 프로젝트 가져오기

1. Vercel 대시보드 → **Add New → Project**
2. GitHub 저장소 목록에서 **single-lab** → **Import**
3. Framework Preset: **Next.js** (자동 감지됨)
4. 아래 **Environment Variables** 펼치기
5. 일단 아래 하나만 넣고 Deploy 눌러도 됨:

| Name | Value |
|---|---|
| `NEXT_PUBLIC_SITE_URL` | `https://single-lab.vercel.app` |

6. **Deploy** 버튼 클릭
7. 2~3분 대기 → 🎉 축하합니다!

이제 전 세계 어디서나 `https://single-lab.vercel.app` 으로 접속됩니다.

---

## 🎯 PART 4 — 도메인 연결 (선택, 10분)

`vercel.app` 주소가 아니라 `single-lab.com` 같은 내 도메인 쓰고 싶다면:

1. 가비아 / 후이즈 / Namecheap 에서 도메인 구매 (연 1~2만 원)
2. Vercel 대시보드 → 프로젝트 → **Settings → Domains → Add**
3. 구매한 도메인 입력 → 안내하는 DNS 설정을 도메인 구매처에 복붙
4. 10분 ~ 24시간 후 연결 완료

---

## 🎯 PART 5 — 수익화 연결하기

### ⚡ 5-1. Google Analytics (가장 먼저 / 5분)

방문자 수를 봐야 뭘 개선할지 알 수 있어요.

1. https://analytics.google.com → 측정 시작
2. 계정 이름: "자취연구소", 속성 이름: "single-lab"
3. 산업 카테고리: "라이프스타일"
4. 비즈니스 규모: 본인에 맞게
5. 플랫폼: **웹** → 스트림 URL 입력 (Vercel 주소)
6. "측정 ID" (G-로 시작) 복사
7. **Vercel → Settings → Environment Variables**
8. 추가: `NEXT_PUBLIC_GA_ID` = `G-XXXXXXXXXX`
9. 상단 **Deployments → 최신 것의 "⋯" → Redeploy**

### ⚡ 5-2. Google Search Console (10분)

구글에서 내 사이트가 검색되게 하는 핵심.

1. https://search.google.com/search-console → 시작하기
2. 속성 유형 **URL 접두어** → 본인 도메인 입력
3. 소유권 확인 방법 → **HTML 태그** 선택
4. `content="XXXXXX"` 부분의 **XXXXXX** 만 복사
5. Vercel 환경변수 추가: `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` = 복사한 값
6. Redeploy 후 Search Console에서 **확인** 버튼 클릭
7. 왼쪽 메뉴 **Sitemaps** → `sitemap.xml` 입력 → 제출

→ 며칠 ~ 2주 후부터 구글에 검색 노출 시작.

### ⚡ 5-3. 네이버 서치어드바이저 (10분)

한국 트래픽의 30~40%는 네이버.

1. https://searchadvisor.naver.com → 로그인
2. **사이트 관리 → 사이트 등록** → 본인 도메인
3. 소유 확인 → **HTML 태그** → content 값 복사
4. Vercel 환경변수 추가: `NEXT_PUBLIC_NAVER_SITE_VERIFICATION` = 복사한 값
5. Redeploy
6. 네이버 돌아가서 **확인** 버튼
7. **요청 → RSS/사이트맵 제출** → `https://본인도메인/sitemap.xml`

### ⚡ 5-4. 쿠팡 파트너스 (15분)

1. https://partners.coupang.com → 가입
2. 사이트 정보 입력 (Vercel 주소)
3. 대부분 1~2일 내 승인
4. 로그인 → **링크 생성기** 검색
5. 추천하고 싶은 상품 검색 → 링크 생성 → URL 복사
6. 프로젝트의 `lib/products.ts` 파일 열기
7. 각 상품의 `coupangUrl: "https://link.coupang.com/a/example-..."` 를 본인 링크로 교체
8. GitHub Desktop에서 변경사항 커밋 & 푸시 → Vercel 자동 재배포

### ⚡ 5-5. Google AdSense (가장 오래 걸림, 1~4주)

**조건: 사이트에 콘텐츠가 충분해야 승인**

승인 전 체크리스트:
- [ ] 블로그 글 최소 20개 이상 (각 1,500자 이상)
- [ ] `/about` 페이지에 연락처, 개인정보처리방침 (이미 있음 ✅)
- [ ] 사이트가 최소 2주 이상 운영 상태
- [ ] 검색 콘솔에 사이트맵 제출됨
- [ ] 최소 하루 10명 이상 방문자

준비되면:

1. https://adsense.google.com → 계정 만들기
2. 사이트 URL 입력 → 심사 신청
3. 심사 중 안내하는 `<script>` 태그를 `<head>`에 넣으라고 함
4. Vercel 환경변수에 `NEXT_PUBLIC_ADSENSE_CLIENT` = `ca-pub-XXXXXXXX` 추가하면 자동 삽입됨
5. `public/ads.txt` 파일 열어서 본인 ads.txt 라인으로 교체
6. Redeploy
7. 심사 대기 (1~4주)

승인 이후:
- AdSense에서 "광고 단위" 만들기 → 슬롯 ID 받기 (숫자)
- 원하는 경우 `components/AdSlot.tsx` 사용 시 `slot="숫자ID"` 전달
- 기본은 **자동 광고**로 설정됨 (Google이 알아서 배치)

---

## 🎯 PART 6 — 콘텐츠 채우기 전략

### 첫 달: 블로그 20개 작성 (가장 중요)

`content/posts/` 폴더에 `.md` 파일 만들면 끝. 예시 3개가 이미 들어있으니 참고.

**고 트래픽 키워드 추천 (검색량 기준):**

1. "자취 시작 체크리스트"
2. "자취방 구하기 꿀팁"
3. "1인가구 식단 1주일"
4. "자취 공과금 아끼는 법"
5. "원룸 곰팡이 제거"
6. "자취 빨래 요령"
7. "다이소 자취템"
8. "자취방 수납 정리"
9. "자취 요리 초보"
10. "자취 가전 추천"
11. "자취 보안 꿀팁"
12. "자취 청소 주기"
13. "원룸 인테리어 소품"
14. "자취생 세금 정리"
15. "월세 계약 주의"
16. "자취 예산 관리"
17. "자취 혼밥 메뉴"
18. "자취 운동 루틴"
19. "원룸 냄새 제거"
20. "자취방 환기 방법"

### 매주: 1~2개씩 꾸준히 업데이트

SEO는 **신선도와 빈도**가 중요. 매주 1개씩만 꾸준히 올려도 6개월 뒤엔 월 방문자 1만 명 돌파 가능.

---

## 🎯 PART 7 — 수익 모델별 예상 수익

### 쿠팡 파트너스

- CPC 기반 아님. **구매 전환** 시에만 커미션(상품가의 3%).
- 월 방문자 1만 → 클릭률 2% → 구매율 5% → 평균 구매 2만원 → **월 약 6만 원**
- 월 방문자 10만 → **월 약 60만 원** 가능

### Google AdSense

- 국가 한국, 라이프스타일 분야 RPM (1,000 조회당 수익): 약 $2~5
- 월 페이지뷰 1만 → **월 약 3~7만 원**
- 월 페이지뷰 10만 → **월 약 30~70만 원**

### 합계 (참고)

- 월 1만 방문: **약 10만 원**
- 월 10만 방문: **약 100만 원 이상**

→ 꾸준히 6~12개월 운영하면 가능한 수치입니다.

---

## 🎯 PART 8 — 자주 쓰는 명령어 치트시트

```bash
# 로컬에서 미리보기
npm run dev

# 배포 빌드 테스트
npm run build
npm start

# GitHub에 변경사항 올리기 (GitHub Desktop 쓰면 필요 없음)
git add .
git commit -m "블로그 추가"
git push
```

---

## 🎯 PART 9 — 트러블슈팅

### 🔴 "npm install" 에러

→ `node -v` 결과가 `v18` 이상인지 확인. 낮으면 Node.js 재설치.

### 🔴 "localhost:3000 연결 안 됨"

→ 다른 프로그램이 3000 포트 사용 중. `npm run dev -- -p 3001` 로 포트 바꿔서 실행.

### 🔴 "Vercel 배포 실패"

→ Vercel 대시보드 → Deployments → 실패 건의 **View Build Logs** → 빨간 에러 라인을 Claude에게 그대로 복붙해서 물어보세요.

### 🔴 "이미지가 안 나와요"

→ 외부 이미지 URL을 쓴다면 `next.config.mjs` 의 `remotePatterns` 에 그 도메인 추가.

### 🔴 "애드센스 계속 거절"

→ **콘텐츠 부족**이 98%. 일기 말고 정보성 글 20개 이상 1,500자 이상으로 채우세요.

---

## 📞 막히면 Claude에게 물어보기

Claude에게 말할 때 도움이 되는 팁:

1. **파일 경로를 말해주세요**: "lib/products.ts 의 power-strip-saver 링크 바꿔줘"
2. **에러는 그대로 복붙**: "이 에러 떠요: [에러 전체 복붙]"
3. **원하는 결과를 말해주세요**: "비용 계산기 결과 아래에 공유 버튼 추가하고 싶어"

---

**화이팅! 한 달 안에 첫 수익 보여드릴 수 있어요. 🚀**
